# 生产环境必须替换为足够长的随机字符串。
MAINRAG_JWT_SECRET_KEY=replace-with-a-long-random-secret

# Token 有效期，单位：分钟。默认 720（12 小时）。
MAINRAG_ACCESS_TOKEN_MINUTES=720

# 可选：设置后，注册教师账号必须填写相同的邀请码。
MAINRAG_TEACHER_INVITE_CODE=
