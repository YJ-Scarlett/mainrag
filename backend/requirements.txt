--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.14
uvicorn[standard]==0.34.3
python-multipart==0.0.20
httpx==0.28.1

# 账号数据库与 JWT 鉴权
SQLAlchemy==2.0.41
PyJWT==2.10.1

pypdf==5.6.1
python-docx==1.2.0
python-pptx==1.0.2
pywin32==311

numpy==2.5.1
torch==2.12.1+cpu
transformers==4.45.2
tokenizers==0.20.3
sentence-transformers==5.1.2
chromadb==0.5.23

# 音视频知识库转写复用 torch + transformers 加载本地 Whisper 模型。
# ffmpeg 不是 pip 包，请按 README 使用 conda 安装：
# conda install -c conda-forge ffmpeg

# 拍照搜题 OCR 可选依赖（二选一即可）：
# 方案一：中文题目识别推荐 PaddleOCR
# python -m pip install paddleocr paddlepaddle
# 方案二：轻量备选 Tesseract，需要额外安装系统版 Tesseract OCR
# python -m pip install pillow pytesseract
